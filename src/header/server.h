#include <iostream>
#include <unordered_map>
#include <functional>

class server
{
private:
    std::string ip;
    std::unordered_map<string, > router;

public:
    server(/* args */);
    ~server();
};
